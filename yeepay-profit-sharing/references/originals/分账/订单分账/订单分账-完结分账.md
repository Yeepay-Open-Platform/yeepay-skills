# 订单分账-完结分账

## 基本信息

- **API ID**: 1fd7ef8994f84dabadf66d135746a6b9
- **API 分组**: divide
- **请求路径**: `/rest/v1.0/divide/complete`
- **请求方法**: POST
- **描述**: 完结分账
- **安全需求**: YOP-RSA2048-SHA256, YOP-SM2-SM3
- **最后更新时间**: 2025-11-10 11:19:37

## 请求参数

### application/x-www-form-urlencoded


| 参数名              | 参数说明    | 类型     | 是否必填 | 描述                                                                          | 示例值                     |
| ---------------- | ------- | ------ | ---- | --------------------------------------------------------------------------- | ----------------------- |
| parentMerchantNo | 发起方商编   | string | 否    | 发起方商户编号。与交易下单传入的保持一致                                                        | sample_parentMerchantNo |
| merchantNo       |         | string | 是    | 商户编号                                                                        | sample_merchantNo       |
| orderId          |         | string | 是    | 商户收款请求号                                                                     | sample_orderId          |
| uniqueOrderNo    |         | string | 否    | 易宝收款订单号                                                                     | sample_uniqueOrderNo    |
| divideRequestId  | 商户分账请求号 | string | 是    | 在商户系统内部唯一（申请分账、完结分账应使用不同的商户分账请求号），同一分账请求号多次请求等同一次。 可包含字母、数字、下划线；需保证在商户端不重复。 | sample_divideRequestId  |
| divideDetailDesc | 分账描述    | string | 否    | 分账完结的原因描述                                                                   | sample_divideDetailDesc |


### 请求示例

#### application/x-www-form-urlencoded

**注意：** 如果存在非ASCII的特殊字符，需要对参数值做URL编码两次，不同编程语言对空格URL编码结果不一样，须统一转为`%20`

```
parentMerchantNo=sample_parentMerchantNo&merchantNo=sample_merchantNo&orderId=sample_orderId&uniqueOrderNo=sample_uniqueOrderNo&divideRequestId=sample_divideRequestId&divideDetailDesc=sample_divideDetailDesc
```

## 响应参数

### application/json


| 参数名                  | 参数说明   | 类型     | 描述                                                                       | 示例值                      |
| -------------------- | ------ | ------ | ------------------------------------------------------------------------ | ------------------------ |
|                      |        | object | 响应结果                                                                     | {}                       |
| └─ code              | 返回码    | string | 返回码,OPR00000 成功                                                          | sample_code              |
| └─ message           | 返回信息   | string | 返回信息                                                                     | sample_message           |
| └─ divideStatus      | 分账状态   | string | 可选项如下: FAIL:失败 SUCCESS:成功 PROCESSING:处理中（正常情况下不会出现，如长期状态未闭环，可联系易宝人工介入处理） | sample_divideStatus      |
| └─ amount            | 分账金额   | number | 完结分账的金额                                                                  | 10.51                    |
| └─ createDate        | 分账创建时间 | string | 分账创建时间                                                                   | sample_createDate        |
| └─ divideSuccessDate | 分账完成时间 | string | 分账完成时间                                                                   | sample_divideSuccessDate |


### 响应示例

#### application/json

```json
{
  "" : "{}",
  "divideStatus" : "sample_divideStatus",
  "amount" : "10.51",
  "code" : "sample_code",
  "message" : "sample_message",
  "createDate" : "sample_createDate",
  "divideSuccessDate" : "sample_divideSuccessDate"
}
```

## 错误码


| 子错误码     | 子错误码描述              | 解决方案                       |
| -------- | ------------------- | -------------------------- |
| OPR00001 | 商户关系有误              | 请根据提示信息，检查入参信息是否正确         |
| OPR00002 | 商户状态有误              | 请根据提示信息，检查入参信息是否正确         |
| OPR00017 | 父商编长度有误             | 请根据提示信息，检查入参信息是否正确         |
| OPR00019 | 子商编长度有误             | 请根据提示信息，检查入参信息是否正确         |
| OPR00030 | 统一订单号长度有误           | 请根据提示信息，检查入参信息是否正确         |
| OPR12002 | 订单不存在               | 请检查订单号是否正确                 |
| OPR15119 | 管控订单未解冻，不可进行分账      |                            |
| OPR21013 | 分账金额超限              | 请根据提示信息，检查入参信息是否正确         |
| OPR21039 | 订单状态不能完结分账          | 请等待易宝通知清算结果或请稍后查询订单状态后，再重试 |
| OPR21040 | 可分账金额为0，不能进行完结分账    | 订单不可分账，请检查订单状态或联系技术支持      |
| OPR21075 | 非法参数:divideDetailNo |                            |
| OPR22001 | 资金处理类型不支持全额确认       | 请根据提示信息，检查入参信息是否正确         |
| OPR26023 | 商户不存在               | 请根据提示信息，检查入参信息是否正确         |
| OPR99999 | 系统异常                | 请稍后重试或联系易宝客服               |


## 示例代码

### JAVA

```java
package com.yeepay.yop.sdk.example;

import com.yeepay.yop.sdk.service.common.YopClient;
import com.yeepay.yop.sdk.service.common.YopClientBuilder;
import com.yeepay.yop.sdk.service.common.request.YopRequest;
import com.yeepay.yop.sdk.service.common.response.YopResponse;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Example {

    // 该Client线程安全，请使用单例模式，多次请求共用
    private static final YopClient client = YopClientBuilder.builder().build();
    private static final Logger LOGGER = LoggerFactory.getLogger(Example.class);

    public static void main(String[] args) {
        YopRequest request = new YopRequest("/rest/v1.0/divide/complete", "POST");
        request.addParameter("parentMerchantNo", "parentMerchantNo_example");
        request.addParameter("merchantNo", "merchantNo_example");
        request.addParameter("orderId", "orderId_example");
        request.addParameter("uniqueOrderNo", "uniqueOrderNo_example");
        request.addParameter("divideRequestId", "divideRequestId_example");
        request.addParameter("divideDetailDesc", "divideDetailDesc_example");

        try {
            YopResponse response = client.request(request);
            LOGGER.info("result:{}", response.getResult());
        } catch (Exception ex) {
            LOGGER.error("Exception when calling, ex:", ex);
        }
    }

}

```

