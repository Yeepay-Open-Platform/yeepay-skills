# 请求签名协议（出站 API）

> Skill 内语言中立协议参考（不使用官方 SDK 时按此实现）。  
> 权威规则：同目录 `鉴权认证机制(RSA).md`、`鉴权认证机制(SM).md`  
> Python 参考实现：`scripts/rsa/client.py`

## Authorization 头格式（RSA）

```text
YOP-RSA2048-SHA256 yop-auth-v3/{appKey}/{timestamp}/{expireSeconds}/{signedHeaders}/{signature}
```

- `timestamp`：UTC `yyyy-MM-dd'T'HH:mm:ss'Z'`
- `signature`：对 canonical request 做 `SHA256withRSA`，再 URL-safe Base64，后缀 `$SHA256`

## 待签名串（canonical request）

五行，以 `\n` 连接：

```text
{authString}
{HTTP_METHOD}
{canonicalURI}
{canonicalQueryString}
{canonicalHeaders}
```

- `authString` = `yop-auth-v3/{appKey}/{timestamp}/{expireSeconds}`
- **GET**：`canonicalQueryString` = 参数 URL 编码后按 key 升序 `k=v&...`
- **POST Form / POST JSON**：`canonicalQueryString` 为空串
- `canonicalHeaders`：参与签名头（含 `x-yop-appkey`、`x-yop-request-id`、`x-yop-content-sha256`、`content-type` 等）按 key 升序，每行 `urlencode(key):urlencode(value)`

## x-yop-content-sha256

| 方法 / 类型 | 计算内容 |
|-------------|----------|
| GET | 空串的 SHA256 hex |
| POST Form | 表单 canonical 串的 SHA256 hex |
| POST JSON | JSON body 原文的 SHA256 hex |

## 报文加密（可选）

敏感字段需加密时，增加 `x-yop-encrypt` 等头；RSA 场景为 RSA 加密随机 AES 密钥 + AES 加密报文。详见 `报文加密机制(RSA).md` 与 example 中 `buildEncryptHeader`。

## 处理步骤（语言中立）

```
1. 构造请求参数 / JSON body
2. 计算 content-sha256，组装参与签名 headers
3. 拼 canonical request 五行
4. signature = RSA_SIGN(canonical_request, 商户私钥) → urlsafe_b64 + "$SHA256"
5. Authorization = 协议前缀 + authString + signedHeaders + signature
6. 附加 SDK 标准头（**不参与签名**，见下节）
7. 发送 HTTP 请求（GET 带 query；POST Form 用 x-www-form-urlencoded；JSON 用 application/json）
```

## SDK 标准请求头（不参与签名）

网关根据 `x-yop-sdk-version` 选择**响应报文格式**。未传或版本过低时，易宝可能按旧版 SDK 返回（无 `x-yop-sign-serial-no` 等新特性）。

| 请求头 | 说明 | 脚本默认值 |
|--------|------|------------|
| `x-yop-sdk-version` | SDK 协议版本（固定） | `4.0.0` |
| `x-yop-sdk-langs` | 语言标识 | `python` |
| `x-yop-session-id` | 会话 ID（UUID） | 进程启动时初始化一次，运行期间不变；进程重启后重新生成 |
| `User-Agent` | 运行时标识（**第二段为 Python 版本**，非 SDK 版本） | `python/{python_version}/{os}/{os_release}/` |

实现：`scripts/common/yop_headers.py`，由 `rsa/client.py`、`sm/client.py` 在签名完成后 `update` 到请求头。

## 易错点

| 项 | 说明 |
|----|------|
| URL 编码 | RFC3986，空格 `%20`；与 `urllib.parse.quote(safe="")` 一致 |
| 签名 Base64 | **URL-safe**，去掉尾部 `=`，后缀 `$SHA256` |
| POST 查询串 | Form/JSON 的 canonicalQueryString 必须为空 |
| 时钟 | timestamp 与服务器偏差过大导致鉴权失败 |
| SDK 版本头 | 自研客户端须带 `x-yop-sdk-version`（脚本固定 `4.0.0`），否则响应可能为旧格式 |
| 环境 | 沙箱网关 `https://sandbox.yeepay.com/yop-center`，生产 `https://openapi.yeepay.com/yop-center` |

## 完整示例（固定测试向量，可逐字节比对）

> 测试私钥：`scripts/rsa/tests/vectors/test_merchant_private.pem`（**仅供测试，禁止真实环境使用**）。
> 全部期望值见 `scripts/rsa/tests/vectors/sign_vectors.json`，由 `scripts/tools/verify_vectors.py` 自动校验，与本节内容保持一致。
> RSA `SHA256withRSA`（PKCS#1 v1.5）签名是**确定性**的：相同密钥+相同待签名串必得相同签名，可逐字节比对定位实现差异。

固定输入（三个用例共用）：

| 项 | 值 |
|----|----|
| appKey | `app_10086032562` |
| timestamp | `2026-01-01T00:00:00Z` |
| expireSeconds | `1800` |
| x-yop-request-id | `00000000-0000-4000-8000-000000000001` |

### 用例 1：GET 查单（query 含空格，验证 %20 编码与 key 升序）

输入：`GET /rest/v1.0/trade/order/query`，参数 `merchantNo=10086032562`、`orderId=TEST ORDER_20260101_001`（注意空格）。

`x-yop-content-sha256`（GET 固定为空串的 SHA256）：

```text
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
```

canonical request（5 行，`\n` 连接；空格已编码为 `%20`）：

```text
yop-auth-v3/app_10086032562/2026-01-01T00:00:00Z/1800
GET
/rest/v1.0/trade/order/query
merchantNo=10086032562&orderId=TEST%20ORDER_20260101_001
x-yop-appkey:app_10086032562
x-yop-content-sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
x-yop-request-id:00000000-0000-4000-8000-000000000001
```

> 第 5 行 canonicalHeaders 自身含换行（每个标头一行），故上块共 7 行文本。

Authorization 头（期望输出）：

```text
YOP-RSA2048-SHA256 yop-auth-v3/app_10086032562/2026-01-01T00:00:00Z/1800/x-yop-appkey;x-yop-content-sha256;x-yop-request-id/bUFNGdhl1AmLzJYhBTRp3G9kuT4gofeyyLwXaR37NTQOA49gjViUtjU_2SmJwX6BRarrmqtSfrNBaEibeyjg8YqMgb_jXEIR8Fl3J0TKRWeRlOEZ74hmJIV_afbUORD_kvG540PpcCAkVZaSNm2bE0zObk3B48LI5Nhu_ZLDZdtcU6AwXDzPg0WKsZCKgACFvVUyQ8FoRRZpyPQtqFkjz6PSWzY3ll7wP4buD3pPB8ebGpTTmxndy3qzu0zGF88S7iDlR6d0Tlx4IA9lL8n1o8_96drIGvWfyP0-Ee-VTdao2eFsFhC6X95na1nGAyipZESGu1MchYE6sumeFjIUVA$SHA256
```

### 用例 2：POST Form 退款（body 含中文，canonicalQueryString 为空串）

输入：`POST /rest/v1.0/trade/refund`，表单参数含 `description=测试退款`（中文）等 5 项。

`x-yop-content-sha256` 对**表单 canonical 串**（urlencode + key 升序 + `&` 拼接）计算；该 canonical 串同时就是请求 body：

```text
description=%E6%B5%8B%E8%AF%95%E9%80%80%E6%AC%BE&merchantNo=10086032562&orderId=TEST_ORDER_20260101_001&refundAmount=0.01&refundRequestId=REFUND_20260101_001
```

```text
a95bf5b62724cdd27e199e50ee2af246d9268f8752d02f0288ad16fc97fef710
```

canonical request（注意第 4 行 canonicalQueryString 为**空串**；content-type 参与签名且值被 urlencode）：

```text
yop-auth-v3/app_10086032562/2026-01-01T00:00:00Z/1800
POST
/rest/v1.0/trade/refund

content-type:application%2Fx-www-form-urlencoded
x-yop-appkey:app_10086032562
x-yop-content-sha256:a95bf5b62724cdd27e199e50ee2af246d9268f8752d02f0288ad16fc97fef710
x-yop-request-id:00000000-0000-4000-8000-000000000001
```

Authorization 头（期望输出）：

```text
YOP-RSA2048-SHA256 yop-auth-v3/app_10086032562/2026-01-01T00:00:00Z/1800/content-type;x-yop-appkey;x-yop-content-sha256;x-yop-request-id/UCVhKDfYjLgziWR1UCmzip4euRxN83KHEV_pq73A_8gieKmkJCRn5-Bvh0h1a1bykVv8HTfJ24MaJQZm6va9vdLXPACcwBvK-huDlvgRRECGu6vwb8GQPit465aWl4hJ_adBR93gCrFIEvYs4FyIola0MnkTk5H9I57976WFaez-D6ZuvodMKaIq-ffDFyCQy10sYnOXqs4lspx_gEI2luNWPFN_4E_U_mmaI8q6Ty4WQXIuDNYTcyQEwR4Jcwz1owo3QacBsiwfvYc36gJROPmHQ7fC87nRAwPtzmf9TGv5Ua0fRYAOZCSpzHbybc39YjZk39FfiOLr-vogoDMJyw$SHA256
```

### 用例 3：POST JSON（content-sha256 取 JSON 原文）

输入：`POST /rest/v1.0/test/json-demo`，body 为 JSON 原文（不可重排/重序列化）：

```json
{"merchantNo":"10086032562","orderId":"TEST_ORDER_20260101_001","orderAmount":"0.01","goodsName":"测试商品"}
```

`x-yop-content-sha256` = 上述 JSON 原文的 SHA256：

```text
3529747095a7e29bcacbbdfa9314f6785f880279cedd629608f9d65a17d3e001
```

canonical request 与 Authorization 结构同用例 2（`content-type:application%2Fjson`，canonicalQueryString 为空串），完整期望值见 `sign_vectors.json` 第 3 个用例。

### SM 双轨说明

SM2 签名带随机数，**每次结果不同**，不能比对签名值，只能用平台公钥验证「verify 通过」；canonical request 与 content-sha256（SM3）的中间值仍可逐字节比对。

## 应答验签（入站 API 响应）

平台对 API 应答（HTTP body）签名，商户用**平台公钥**验签，与出站请求签名方向相反。

| 项 | 说明 |
|----|------|
| 响应头 | `x-yop-sign`（必填才有验签意义）；SM2 另有 `x-yop-sign-serial-no` 标识平台证书 |
| 待签原文 | 响应 body **去掉所有空格、制表符、换行**后的 UTF-8 字节（`re.sub(r'[ \t\n]', '', body)`） |
| 签名格式 | `{urlSafeBase64Sig}$SHA256`（RSA）或 `{urlSafeBase64Sig}$SM3`（SM2）；亦可能为标准 Base64（含 `+` `/` `=`） |
| RSA 公钥 | 控制台复制的易宝 RSA 公钥；SM2 见 `平台商密证书.md` |
| 无签名头 | 部分接口/环境不返回 `x-yop-sign`，可跳过验签 |

处理步骤：

```
1. 读取 response.text 与 headers['x-yop-sign']
2. normalized = 移除 body 中空格/制表符/换行
3. 解析 x-yop-sign → 签名值 + 摘要算法（$ 分隔）
4. RSA：SHA256withRSA 验签；SM2：SM3withSM2 验签（gmssl verify_with_sm3）
5. SM2 按 x-yop-sign-serial-no 选择平台公钥（多证书缓存）
```

Python 实现：`scripts/common/response_verify.py`；`rsa/client.py` / `sm/client.py` 的 `call(..., verify=True)`；CLI `scripts/tools/verify_response.py`。

应答验签测试向量：`rsa/tests/vectors/response_vector.json`、`sm/tests/vectors/response_sm_vector.json`（`tools/verify_vectors.py` 校验）。

## 本地验证

```bash
python tools/verify_vectors.py              # 校验本节向量与脚本实现一致
python query_order.py --dry-run ...   # 打印 canonical request 与 Authorization
```

对照 `鉴权认证机制(RSA).md` 与 example 源码逐步比对。
